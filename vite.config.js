import { defineConfig } from 'vite'
import react from '@vitejs/plugin-react'

// https://vite.dev/config/
export default defineConfig({
  plugins: [react()],
  base: '/qr-living-canvas/', // Exact GitHub Pages subpath URL repository name
})
